{
  "name": "jarvis-assistant",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node -e \"console.log('Nova static assets are ready')\"",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
